pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        jcenter()
    }
}

rootProject.name = ("io.github.simonscholz.github.release.notes")

include(":plugin")
